# Configuración para MongoDB Atlas (cloud)
# Las credenciales se toman del archivo .env en el directorio microservicios/
spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/empleados-db}
